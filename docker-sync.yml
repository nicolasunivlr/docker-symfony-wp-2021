version: '2'
syncs:
  app-sync:
    src: ${APP_PATH}
    sync_strategy: 'native_osx'
    sync_userid: ${USERID}
    sync_excludes:
      - var
      - ".idea"
      - ".git"
      #- node_modules
      #- vendor
      